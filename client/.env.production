# Production build settings for Vite (used by `vite build`)
# IMPORTANT: This must point to your deployed server's public URL.
VITE_POS_BASE_URL="https://capstone-production-fe8d.up.railway.app"

# If you truly use an API key, set it here (not a URL). Otherwise, leave it empty or remove it.
# VITE_POS_API_KEY=""
